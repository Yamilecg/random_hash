name: Run Hash Generator

on: [push]

jobs:
  run:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v3
      - name: Run Python script
        run: python hash_generator.py